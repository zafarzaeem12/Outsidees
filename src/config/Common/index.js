const baseURL = 'https://server.appsstaging.com/3148/the_handrail_app/api/';
const socketURL = 'https://server.appsstaging.com:3035/';

const defaultTimeout = 30000;

export default {baseURL, socketURL, defaultTimeout};
